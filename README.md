# aws-invoice-processing-module
A reference implementation of a small AWS-based invoice ingestion workflow that accepts vendor invoice PDFs, extracts structured data, validates content, stores invoice and vendor records, and generates double-entry accounting entries.
